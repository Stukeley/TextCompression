/**
 * The 'models' package contains classes responsible for executing the algorithm, as well as helper functions
 * that determine which algorithm to run and responsible for checking correctness of data.
 * It contains an exception class, objects of which are thrown when user input is not correct.
 * @author Rafał Klinowski
 * @version 1.0
 *
 */
package pl.polsl.models;